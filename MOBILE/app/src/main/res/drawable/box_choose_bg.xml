<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền -->
    <solid android:color="#E5D9D9" />

    <!-- Bo tròn 4 góc -->
    <corners android:radius="20dp" />

    <!-- Viền nhẹ (tùy chọn) -->
    <stroke
        android:width="1dp"
        android:color="#D0C4C4" />
</shape>
