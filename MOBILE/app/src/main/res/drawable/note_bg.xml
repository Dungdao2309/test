<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền -->
    <solid android:color="#F3CBCB" />

    <!-- Bo tròn góc -->
    <corners android:radius="16dp" />

    <!-- Viền nhẹ (tùy chọn) -->
    <stroke
        android:width="1dp"
        android:color="#D9AFAF" />
</shape>
